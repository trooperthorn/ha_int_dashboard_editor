pytest-homeassistant-custom-component==0.13.364
# The harness leaves the frontend package out; the panel tests need it, and the pin must equal the core pin in package_constraints.txt.
home-assistant-frontend==20260826.6
ruamel.yaml==0.19.1
mypy==2.3.1
ruff==0.16.6
